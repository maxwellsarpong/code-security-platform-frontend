# Optional: URL of the dashboard app (used after sign-in redirect).
# In local dev, landing and dashboard run on different ports, e.g.:
# VITE_DASHBOARD_URL=http://localhost:5174
# In production, set to your dashboard origin or path (e.g. https://app.example.com).
